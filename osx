# Configuration for OSX

# If boot2docker is installed and running, load up the env vars
if [[ -e /usr/local/bin/boot2docker ]]
then
  if [[ `/usr/local/bin/boot2docker status` == 'running' ]]
  then
    eval $(/usr/local/bin/boot2docker shellinit 2>/dev/null)
  fi
fi

